---

# 提分前必读

> 💡 **比赛官网**：[https://competition.ai4s.com.cn/m/9?channelCode=dw](https://competition.ai4s.com.cn/m/9?channelCode=dw)

## 核心目标

本任务的核心是：**持续测试策略、提分**

这个阶段最主要的目标就是不断思考策略，尝试优化结果上分！

> ⚠️ 注意：这份教程是一份**社区笔记**，不是标准答案。

> 💡 过程中推荐使用 AI 工具辅助学习和解题（点击右边三角形标志可打开看到全部内容）

---

## 提交规则

⚠️ 比赛期间，**每天有 3 次提交机会**

**策略**：本地多跑几个改进版本 → 挑当天最值得验证的 1~3 版提交 → 看分数反馈 → 第二天调整方向继续

---

## 1. 本地有两个评估指标

你跑 Baseline 的时候，终端会打印这两段（可以翻一下截图）：

```text
验证集 RMSE: 0.xxxxxx, MAE: 0.xxxxxx
```

```text
总收益: xxxx.xx
平均日收益: xxxx.xx
```

这两个数字就是你本地的"指标"：

| 指标 | 含义 | 怎么看 |
|------|------|--------|
| **验证集 RMSE** | 模型预测电价的平均误差 | **越小越好**——越接近 0，预测越准 |
| **平均日收益** | 按你的策略，平均每天能赚多少 | **越大越好**——数字越大，策略越赚钱 |

> 💡 **经验规律**：本地的 RMSE 降低 + 平均日收益涨，真实提交分数大概率也会涨。所以每次改完代码，先看终端这两个数字，再决定要不要把这一版交上去。

---

## 2. 每天 3 次提交，建议这样分配

| 次数 | 用途 | 该交什么 |
|------|------|----------|
| **第 1 次** | 验证今天最有把握的改进 | 挑 **RMSE 最低、日收益最高** 的那一版，看真实分数是不是真的涨了 |
| **第 2 次** | 验证一个还在犹豫的改进 | 比如"加 XX 特征到底有没有用"，交个对照版直接看真实分数差 |
| **第 3 次** | 看完前两次反馈再决定 | 👉 如果前两次都涨分了：把两个改进合在一起交一版，看是不是涨得更多<br>👉 如果前两次都没涨甚至跌了：退回原版、只改一个小地方（比如删掉一个明显没用的特征），确认你的本地评估指标还能不能预测真实分数 |

### 第 3 次提交的进一步判断

- 如果本地 RMSE 降 / 日收益涨、赛事官网的真实分数也涨 ✅ → 本地指标没问题，前两次只是方向选错了，可以继续按本地指标试
- 如果本地 RMSE 降 / 日收益涨、赛事官网的真实分数反而降 ❌ → 说明本地数字看着好，实际不一定真的好——你的本地 RMSE / 日收益 和赛事官网分数已经"对不上"了。这种情况下别再瞎试，得停下来重新想办法

> 💡 **经验规律**：每天的 3 次最好分散一点用——别一上来就把 3 次全交了，最好每次提交后等到分数出来再交下一次。这样你能在当天就形成 **"改 → 提 → 看 → 改"** 的快循环，迭代速度最快。

---

## 3. 每次实验都记一笔

建议你搞一个实验记录表（Markdown / 记事本 / 飞书随便）。否则改了几轮、跨了几天后，你会忘记哪天交了哪版、哪版得了多少分。

### 示例记录表

| 日期 | 第几次 | 版本 | 改了什么 | 验证集 RMSE | 平均日收益 | 提交得分 |
|------|--------|------|----------|-------------|------------|----------|
| 4/25 | #1 | v1 | Baseline 原版 | 0.6478 | 11618 | 0.32 |
| 4/25 | #2 | v2 | + 滞后特征 | 0.5921 | 12340 | 0.38 |
| 4/25 | #3 | v3 | v2 + 删 month 特征 | 0.5905 | 12380 | 0.38（几乎一样） |
| 4/26 | #1 | v4 | v2 + 气象数据 | 0.5503 | 13280 | 0.45 |

---

## 4. Baseline 还有哪些可以改进的地方？

回顾一下，Baseline 在三个层面都做得很简单：

### ① 特征（模型用了什么信息）

- 只用了 7 个"预测值"特征，训练集里的"实际值"特征完全没用到
- 气象数据（`.nc` 文件）完全没用
- 没有历史电价的滞后特征（比如"昨天同一时刻的电价"）

### ② 模型（用什么算法学规律）

- 用的是基础的 `GradientBoostingRegressor`（梯度提升树）
- GBDT 模型本身不感知时序顺序，需要通过特征工程（比如滞后特征、滚动统计）把时间信息显式喂给它

### ③ 策略（根据预测决定怎么充放电）

- 过度依赖预测电价：无论预测置信度高低，都按预测结果直接执行充放电——预测有偏差时，策略也会跟着出错
- 缺少稳健性设计：没有"预测不可靠时降低操作激进度"的兜底机制

